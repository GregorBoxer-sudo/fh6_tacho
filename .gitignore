# Rust build output
/target/

# Learned car data and drive recordings — recreated at runtime
/data/

# Logs
/logs/

# Distribution package
/dist/
*.zip

# Atomic-write temp files — should never exist at rest, excluded as a safety net
*.tmp
